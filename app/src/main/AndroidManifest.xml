<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication1">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TryCatchActivity" />
        <activity android:name=".DownloadImage" />
        <activity android:name=".ImageUpload" />
        <activity android:name=".GetTodsData" />
        <activity android:name=".PostUserFullDetailData" />
        <activity android:name=".GetRetrofitImage" />
        <activity android:name=".PostRetrofitData" />
        <activity android:name=".BridgeActivity" />
        <activity android:name=".ListLoanPage" />
        <activity android:name=".GetRetrofitData" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>