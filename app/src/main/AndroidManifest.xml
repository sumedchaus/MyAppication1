<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication1">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>


    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
    android:theme="@style/AppTheme">
        <activity android:name=".ImageUpload"></activity>
        <activity android:name=".GetTodsData"></activity>
        <activity android:name=".PostUserFullDetailData"></activity>
        <activity android:name=".GetRetrofitImage"></activity>
        <activity android:name=".PostRetrofitData"></activity>
        <activity android:name=".BridgeActivity"></activity>
        <activity android:name=".ListLoanPage"></activity>
        <activity android:name=".GetRetrofitData"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>